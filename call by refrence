#include <stdio.h>
#include <string.h>

int main() {
    int a=10,b=20;
    int * p1 *,p2,temp;
   * p1= &a;
   * p2= &b;
    printf("%d %d\n",&a,&b);
    temp=*p1;
   * p1= *p2;
    temp=*p2;
    printf("%d %d\n",&a,&b);
}
