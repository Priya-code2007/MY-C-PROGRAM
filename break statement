#include <stdio.h>

int main() {
    for (int i = 1; i <= 10; i++) {
        if (i == 5) {
            printf("Break at i = %d\n", i);
            break;   // exit the loop when i reaches 5
        }
        printf("i = %d\n", i);
    }
    printf("Loop ended.\n");
    return 0;
}
