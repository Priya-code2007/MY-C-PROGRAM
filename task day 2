#include <stdio.h>
int main(){
int  sum,diff,prod,quotient, x,y;
printf("enter the value of x and y");
scanf("%d%d",&x,&y);
sum=x+y;
diff=x-y;
prod=x*y;
quotient=x/y;
printf("sum=%d,\ndiff=%fd,\nprod=%d,\nquotient=%d,\nsum,diff,prod,quotient");
    
}

